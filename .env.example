# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=
TIME_ZONE=UTC
LOG_LEVEL=INFO

# Database (defaults to local SQLite when unset)
# DATABASE_URL=postgres://user:password@localhost:5432/nanobanana

# Telegram
TELEGRAM_BOT_TOKEN=
ADMIN_USER_ID=0
BOT_ALLOW_EVERYONE=False
BOT_POLL_INTERVAL=3

# Encrypts user prompts and generated images at rest.
# Generate with: python manage.py generate_encryption_key
# Back it up separately — without it, stored prompts cannot be decrypted.
PROMPT_ENCRYPTION_KEY=

# Kie.ai image generation
KIE_API_KEY=
KIE_MODEL=google/nano-banana
CALLBACK_URL=https://example.com/api/callback
